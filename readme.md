# Setu — Phase 1 Skeleton (Clean Workspaces)

This version scopes dependencies to each workspace (no React/xterm/node-pty in the root).
Use `npm run dev` from the root to start everything.

## Requirements
- Node.js 20+
- npm 9+

## Run
```bash
npm install
npm run dev
```
